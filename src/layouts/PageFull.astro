---
import Default from "@layouts/Default.astro";

const { title } = Astro.props;
---

<Default title={title}>
  <div class="title-font mb-2 text-sm uppercase tracking-widest text-gray-500">
    <i class="bi bi-house mr-1"></i>
    {title}
  </div>

  <div class="max-w-[50rem]">
    <slot />
  </div>
</Default>
